# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Session-local runtime state, not project source
.claude/scheduled_tasks.lock
.impeccable/live/

# An unrelated external repo (tommyjepsen/awesome-ux-skills) that ended up
# cloned into this project directory — not FraXtal source, excluded rather
# than committed as a broken embedded repo.
/awesome-ux-skills
